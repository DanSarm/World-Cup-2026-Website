NEXT_PUBLIC_SITE_NAME="Family Cup 2026"
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_publishable_key
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_supabase_secret_key
SESSION_SECRET=your_random_32_char_secret_minimum
FAMILY_ACCESS_CODE=your_family_code
ADMIN_INVITE_CODE=your_admin_code

# The Odds API (optional — bonus points still work with manual admin entry)
# Free tier = 500 credits/month. Keep ODDS_REGIONS=us (1 credit per odds call).
ODDS_API_KEY=
ODDS_API_PROVIDER=the_odds_api
ODDS_REGIONS=us
ODDS_WINNER_REGIONS=us
ODDS_MARKETS=h2h,draw_no_bet
ODDS_FORMAT=decimal
ODDS_LOCK_HOURS_BEFORE_KICKOFF=1
# Automatic match-odds refresh (free sources — no credits)
POLYMARKET_ODDS_MIN_INTERVAL_MS=120000
POLYMARKET_ODDS_URGENT_INTERVAL_MS=60000
ODDS_SYNC_MIN_INTERVAL_MS=300000
ODDS_SYNC_URGENT_INTERVAL_MS=120000
ODDS_SYNC_MAX_PER_DAY=48
# Set true only if you want paid Odds API used after free sources (costs credits)
ODDS_USE_PAID_MATCH_ODDS=false
ODDS_SPORT_KEY=soccer_fifa_world_cup
ODDS_WINNER_SPORT_KEY=soccer_fifa_world_cup_winner
CRON_SECRET=

# Web Push — free browser/phone pick reminders (15 min before kickoff)
# Generate keys: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com

# Live scores — ESPN is primary (free). Odds API is fallback for odds credits.
LIVE_SCORES_ENABLED=true
LIVE_SCORES_MAX_SYNCS_PER_DAY=8
ODDS_API_MIN_CREDITS_RESERVE=40
# Client poll while matches may be live (default 10s)
NEXT_PUBLIC_LIVE_POLL_INTERVAL_MS=10000
# Picks page poll when no live matches (default 45s)
NEXT_PUBLIC_PICKS_POLL_INTERVAL_MS=45000
# Faster picks poll while win percentages are still loading (default 30s)
NEXT_PUBLIC_ODDS_POLL_INTERVAL_MS=30000
# ESPN live scores — free, no API key (default 10s server sync)
ESPN_LIVE_SCORES_MIN_INTERVAL_MS=10000
# ESPN moneyline odds fallback — free (default 2 min)
ESPN_ODDS_MIN_INTERVAL_MS=120000
# The Odds API scores fallback only — preserve credits for odds (default 15 min)
LIVE_SCORES_MIN_INTERVAL_MS=900000
